var require_config = {
    baseUrl: '/js/modules',
    paths: {
        lib: '/js/lib',
        jquery: '/js/lib/jquery-2.0.3.min'
    }
};

// Init requirejs config
requirejs.config(require_config);
